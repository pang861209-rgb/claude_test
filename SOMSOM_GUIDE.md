# 솜솜 약먹핑 — 끼토 이미지 제작·적용 가이드 🐰

이 브랜치(`somsom-yakmeokping`)는 **솜솜시티 IP** 버전이에요.
앱 이름이 "솜솜 약먹핑"으로 바뀌었고, 마스코트 자리에 **끼토(분홍 토끼)** 이미지를 끼울 수 있는 구조가 준비돼 있어요.

> **현재 상태 ✅**: 끼토 적용 완료! 작가 원본 시트(`somsom_assets/sheet_kkito.png`)에서
> 포즈 3종을 그대로 잘라내(그림체 변형 0%) 배경 투명 처리 후 애셋에 넣었어요.
> - idle = 정면 기본 서기 / waiting = 손 흔들기 / happy = 꽃다발 🌷
> - 더 높은 해상도나 새 포즈(만세 점프 등)를 원하면 gen2.py로 생성해 교체하면 됨 (아래 참고)

## 왜 이미지를 직접 넣는 구조인가

작가 시트 규칙 때문이에요:

> "텍스트 묘사만으로 만들지 말고, 해당 캐릭터의 원본 시트를 이미지 입력으로 직접 넣어라.
> 재디자인 금지, 색은 시트에서 샘플링, 선·비율·이목구비 동일하게."

이 원격 개발 환경에서는 맥북의 `~/Downloads/솜솜시티_IP/` 시트에 접근할 수 없어서,
그림체 변형 위험이 있는 "텍스트 기반 재현"은 하지 않았어요. 대신 **시트 기반으로 생성한
PNG를 그대로 꽂는 슬롯**을 만들어 뒀습니다.

## 1. 필요한 이미지 3장

`gen2.py`(시트 입력 방식)로 아래 3개 포즈를 생성하세요. 입력 시트: `sheet_kkito.png`

| 파일 용도 | 포즈 요청 (프롬프트에 쓸 내용) | 쓰이는 곳 |
|---|---|---|
| **idle** (평소) | 정면으로 서서 편안하게 살짝 미소 짓는 전신 포즈 | 홈 화면, 스플래시 |
| **waiting** (약 시간) | 신나서 기대하는 표정, 한 손(앞발)을 살짝 든 전신 포즈 | 약 먹을 시간의 홈 화면 |
| **happy** (인증 완료) | 활짝 웃으며 두 팔 벌려 기뻐하는(만세) 전신 포즈 | 축하 화면, 온보딩 인사 |

**생성 조건 (프롬프트에 꼭 포함)**:
- `sheet_kkito.png`를 **이미지 입력으로 첨부** (텍스트 설명만으로 생성 금지)
- "원본 시트의 그림체·선 굵기·비율·이목구비를 그대로 유지. 재디자인 금지. 색은 시트에서 샘플링"
- **배경 투명(transparent background) PNG**, 전신, 정사각형에 가깝게, 1024×1024px 내외
- 여백: 캐릭터가 캔버스의 80~90%를 차지하게

> 💡 배경 투명이 안 되면 흰 배경으로 생성한 뒤 맥 미리보기(Preview) →
> 마크업 도구 → 인스턴트 알파로 배경을 제거해도 돼요.

## 2. Xcode에 넣기 (드래그 한 번)

1. Xcode에서 프로젝트 열기 → 왼쪽에서 `Yakmeokping/Assets` 클릭
2. 애셋 목록에서 아래 3개의 비어있는 슬롯을 찾아요:
   - `somsom_kkito_idle`
   - `somsom_kkito_waiting`
   - `somsom_kkito_happy`
3. 각 슬롯의 **2x 칸**에 해당 PNG를 드래그 앤 드롭 (1x/3x는 비워둬도 됨)
4. 실행(▶) — 홈·온보딩·축하 화면에 끼토가 나타나요! 🎉

## 3. 자동으로 적용되는 것들

- 온보딩 인사: "안녕! 나는 **끼토**야 💗"
- 후속 알림 문구: "**끼토**가 아직 기다리고 있어요 🥺"
- 앱 이름: 홈 화면에 **"솜솜 약먹핑"**으로 표시
- 숨쉬기 통통·인증 시 폴짝 점프 애니메이션은 이미지에도 동일하게 적용

## 4. 나중에 확장하고 싶다면 (선택)

- **앱 아이콘 교체**: 끼토 얼굴 1024×1024 PNG(불투명, 배경 있음)를 만들어
  `Yakmeokping/Assets.xcassets/AppIcon.appiconset/AppIcon.png`를 교체
- **다른 캐릭터 등장**: 축하 화면에 보늘·미개·람다·라칠친 할머니·북거씨가 번갈아 나오게 하려면
  같은 방식으로 시트 기반 이미지를 생성해서 요청해 주세요 — 코드는 제가 붙여드릴게요
  (북거씨는 시트 규칙대로 직립 ❌, 네 발 거북 + 목 터틀넥 + 직사각 안경 포즈로 생성)
- **캐릭터 이름/앱 이름 변경**: `Yakmeokping/Design/Branding.swift` 한 파일만 고치면 돼요

## 브랜치 구분

| 브랜치 | 내용 |
|---|---|
| `claude/medication-verification-app-mq7ofn` | 원본 약먹핑 (오리지널 핑핑이) |
| `somsom-yakmeokping` | 솜솜시티 IP 버전 (끼토) ← 지금 여기 |
